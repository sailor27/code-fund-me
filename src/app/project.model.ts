export class Project {
  constructor (public title: string, public image: string, public creators: string, public description: string, public category: string, public goal: string, public plans: string, public swag: string) { }
}
