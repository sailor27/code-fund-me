import { FilterByGoalPipe } from './filter-by-goal.pipe';

describe('FilterByGoalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByGoalPipe();
    expect(pipe).toBeTruthy();
  });
});
